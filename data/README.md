# Data Directory

This directory contains runtime data files.

## Files

- `claims.json` - Listing claim submissions (auto-created, gitignored for privacy)
- `raw/` - Raw crawler data dumps (gitignored)

## Note

The `claims.json` file is automatically created when the first claim is submitted.
It contains PII (email, phone) and should not be committed to version control.
